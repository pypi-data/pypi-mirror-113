from setuptools import setup

setup(name='ich_distributions',
      version='0.1',
      description='Gaussian distributions',
      packages=['ich_distributions'],
      author = 'lchh',
      zip_safe=False
)
