from setuptools import setup

setup(name='mamanteo_distributions',
      version='0.1',
      description='Binomial and Gaussian Distributions',
      packages=['mamanteo_distributions'],
      zip_safe=False)
