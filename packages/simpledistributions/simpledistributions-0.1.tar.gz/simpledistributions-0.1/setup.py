from setuptools import setup

setup(name='simpledistributions',
      version='0.1',
      description='Gaussian, Poisson and Binomial distributions',
      packages=['simpledistributions'],
      zip_safe=False)
