from setuptools import setup

setup(name='distributions-gabrielmarafon',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['distributions-gabrielmarafon'],
      zip_safe=False)
